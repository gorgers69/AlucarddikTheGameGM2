function scr_player_dash(){
	hsp = movespeed * image_xscale;
	if floor(image_index) == (image_number - 1)
	{
		suplex = 0;
		state = states.normal;
	}
	if sprite_index = spr_player_shot
		movespeed -= 0.1;
}